#summary One-sentence summary of this page.

= L_Label =
{{{
    case class L_Label(label : String) extends L_Node
    implicit def stringToLabel(s : String) : L_Label = L_Label(s)
}}}

Used for creating new [L_Block block] labels in SLEM.